# HTTPServer_Library
A portable networking library written in C++ with support for TCP server and HTTP server implementations. Features platform-specific optimizations for Windows, macOS, and Linux, includes a multi-threaded HTTP server with SSL/TLS support
